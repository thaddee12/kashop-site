# .env.example — Template public des variables d'environnement
#
# Copier ce fichier en ".env" et remplir les valeurs.
# Ce fichier peut être commité, il ne contient aucune clé réelle.

# ── FTP Hostinger (déploiement) ─────────────────────────────────────────────
# Ces valeurs vont dans les secrets GitHub (Settings > Secrets and variables >
# Actions), pas dans un .env local : FTP_HOST, FTP_USER, FTP_PASS.
# Repo : https://github.com/thaddee12/kashop-site
